import React from "react";
import Loading from "../loading";
import NotFound from "../not-found";

const Page = () => {
  return (
    <div>
      <NotFound />
    </div>
  );
};

export default Page;
